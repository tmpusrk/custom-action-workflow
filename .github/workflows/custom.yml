name: Custom Action

on:
  push:
    branches: [main]

jobs:
  test:
    name: Test Custom Action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Use Custom Action
        uses: tmpusrk/my-custom-action@main
    
  
